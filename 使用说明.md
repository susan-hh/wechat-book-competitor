# 短视频带货竞品分析工具 · 使用说明

本工具用于跟踪视频号上图书带货竞品的表现：记录了哪些账号在卖、每天卖了多少、哪些视频成了爆款，并自动算出增长趋势和销量排名。全部数据存在你自己的电脑浏览器里，不上传任何服务器。

它由三张关联表 + 数据看板组成：

- **竞品账号库**：账号 / 平台 / 首次发现 / 开通天数 / 主页链接 / 橱窗链接 / 粉丝量 / 内容领域 / 主要商品 / 账号定位
- **每日数据记录**：日期 / 账号 / 视频标题 / 视频类型 / 商品 / 当前销量 / 昨日销量 / 新增销量 / 点赞 / 评论 / 播放 / 是否爆款
- **爆款案例库**：账号 / 视频标题 / 封面标题 / 开头3秒 / 内容结构 / 情绪点 / 商品 / 成交效果 / 可复制经验
- **数据看板**：① 增长最快账号（默认近1天）② 按日期增长趋势 ③ 销量最高商品

---

## 〇、关于这个分享版（必读）

- **数据完全独立，不与任何主版本同步。** 这个分享版是本地单机工具，你的竞品数据只存在你自己浏览器的 localStorage 里，不会自动从任何地方拉取，也不会上传到任何服务器。原作者（GitHub 发布者）的数据和你毫无关联。
- **初始只有 1 条示例数据。** `data.js` 里预置了「六月人物漫谈」账号 + 3 天销量，仅用于演示格式。看清后点「清空本地数据」即可开始你自己的记录。
- **从 GitHub 下载后怎么用。** 把整个文件夹（html + data.js + 使用说明.md）下载到本地任意位置，三者必须放在同一目录。双击 html 打开即可，不需要安装任何东西、不需要起服务器。
- **如果你用 WorkBuddy 辅助录入**（推荐）：把本说明的「方式一」整体发给你的 WorkBuddy，它就能按标准格式帮你整理数据。WorkBuddy 若有文件夹访问权限，可直接帮你写【本分享版目录下】的 `data.js`——注意只写这个文件，不要去碰任何"主版/主目录"的 data.js（本分享版不依赖它们）。

---

## 一、你拿到了什么

一个「分享版」文件夹，里面 3 个文件，必须放在一起：

| 文件 | 作用 |
|------|------|
| `竞品分析工具_分享版_空模板.html` | 主程序，双击用浏览器打开 |
| `data.js` | 初始示例数据（含一个示例账号），**必须和上面的 html 放在同一目录** |
| `使用说明.md` | 本说明文档 |

> 注意：`html` 和 `data.js` 缺一不可，且要在同一个文件夹里。只拷走 html 单独打开会是空白。

---

## 二、第一次打开

1. 双击 `竞品分析工具_分享版_空模板.html`。
2. 用 **Chrome 或 Edge** 浏览器打开（不要用老版 IE）。
3. 打开后默认会显示一条**示例账号「六月人物漫谈」**（含 3 天的销量记录），方便你看清楚每个字段怎么填。

---

## 三、开始录入你自己的竞品

你有两种录入方式，可任选其一，也可混用。怎么选：

- **首次建账号 + 批量补历史数据**（如一次性把某个账号的近几天销量都建好）：方式一（WorkBuddy）省力，方式二手动也行。
- **每天新增一条当日销量**：推荐直接用方式二在界面上点一下，最快最即时，不必绕 WorkBuddy 一趟。

### 方式一：让 WorkBuddy 帮你自动录入（最省力）

你（接收方）的操作顺序：
1. 双击 `竞品分析工具_分享版_空模板.html` 打开工具，看一眼示例账号「六月人物漫谈」，建立直觉。
2. 把下方「工具说明书」整体发给**你自己的 WorkBuddy**，让它先"认识"这个工具（只需一次）。
3. 之后每次把竞品数据用大白话发给 WorkBuddy，它会返回标准 JSON；你把 JSON 存成 `.json` 文件，在工具里点「导入JSON」即可。

> 说明：WorkBuddy 不需要、也不会去打开这个 HTML 文件。它"知道"这个工具，靠的是你发给它的文字说明书（JSON 格式 + 规则）；它做的事是把你的口语化竞品信息整理成可导入的 JSON，最后由你导入。两者无需直接连接。

不用自己一个个填表单。让 WorkBuddy 把竞品信息整理成工具能直接导入的 JSON，再用「导入JSON」一键加载。

**第一次用，要先让 WorkBuddy 认识这个工具**（否则它不知道要输出什么格式、按什么规则算）。把下面的"工具说明书"整体发给你的 WorkBuddy 一次即可；它确认了解后，之后每次直接发竞品数据就行：

> 你接下来协助我使用一个本地 HTML 工具「短视频带货竞品分析工具」（分享版，用于跟踪视频号图书带货竞品）。我给你竞品账号和每日销量，请你整理成它能「导入JSON」加载的标准 JSON。
>
> 重要：这是分享版，数据完全独立、本地存储，不与任何主版本同步。data.js 里只有示例数据，我自己的竞品数据需要自行录入。如果你有文件夹访问权限要帮我写 data.js，只写【本分享版目录下】的 data.js（和 html 同目录的那个），不要去碰任何主版/主目录的 data.js。
>
> 结构：
> ```
> {
>   "accounts": [{"账号名称":"","平台":"视频号","首次发现":"YYYY-MM-DD","发现时已开通":数字,"主页链接":"","橱窗链接":"微信小店带货橱窗地址","粉丝量":0,"内容领域":"","主要商品":"","账号定位":"","备注":""}],
>   "daily": [{"日期":"YYYY-MM-DD","账号":"","视频标题":"","视频类型":"","商品":"","当前销量":数字,"昨日销量":数字,"新增销量":数字,"点赞数":0,"评论数":0,"播放量":0,"是否爆款":"否","备注":""}],
>   "cases": []
> }
> ```
>
> 规则：1) 新增销量 = 当前 − 昨日；但「发现当天」（日期等于账号首次发现日）新增销量填 0。2) 商品名暂未确认的填「待确认」。3) 橱窗地址格式 `#微信小店带货橱窗://账号名的橱窗/xxxx`。4) 只返回纯 JSON，方便我直接存成 .json 导入。
>
> 收到后请回复"已了解，请发送竞品数据"。

**之后日常录入**：直接把竞品信息用大白话发给 WorkBuddy 即可。以「六月人物漫谈」为例，把下面这段话发给它：

> 帮我录入一个视频号图书带货竞品：
> - 账号：六月人物漫谈，平台视频号，首次发现 2026-08-14，发现时已开通 0 天，内容领域 历史文化类，主要商品 国之脊梁
> - 橱窗链接：#微信小店带货橱窗://六月人物漫谈的橱窗/hgPOZj9pRKSX2Kq
> - 每日销量：8月14日 439件，8月15日 458件，8月16日 478件
> 请按「短视频带货竞品分析工具」的标准 JSON 格式（accounts + daily 两部分）整理好发我，新增销量按规则算（发现当天记 0，其余 = 当前 − 昨日）。

WorkBuddy 会返回一段 JSON（形如下方），你把它保存成 `.json` 文件，再在工具里点「导入JSON」选中它，账号和每日记录就一次性建好了：

```
{
  "accounts": [
    {"账号名称":"六月人物漫谈","平台":"视频号","首次发现":"2026-08-14","发现时已开通":0,"主页链接":"","橱窗链接":"#微信小店带货橱窗://六月人物漫谈的橱窗/hgPOZj9pRKSX2Kq","粉丝量":0,"内容领域":"历史文化类","主要商品":"国之脊梁","账号定位":"历史文化带货","备注":"微信小店名：六月人物漫谈的橱窗"}
  ],
  "daily": [
    {"日期":"2026-08-14","账号":"六月人物漫谈","视频标题":"","视频类型":"","商品":"待确认","当前销量":439,"昨日销量":0,"新增销量":0,"点赞数":0,"评论数":0,"播放量":0,"是否爆款":"否","备注":""},
    {"日期":"2026-08-15","账号":"六月人物漫谈","视频标题":"","视频类型":"","商品":"待确认","当前销量":458,"昨日销量":439,"新增销量":19,"点赞数":0,"评论数":0,"播放量":0,"是否爆款":"否","备注":""},
    {"日期":"2026-08-16","账号":"六月人物漫谈","视频标题":"","视频类型":"","商品":"待确认","当前销量":478,"昨日销量":458,"新增销量":20,"点赞数":0,"评论数":0,"播放量":0,"是否爆款":"否","备注":""}
  ],
  "cases": []
}
```

#### 方式一增强：WorkBuddy 有工具文件夹访问权限时（可直接写入 data.js）

如果 WorkBuddy 能直接读写工具所在文件夹（比如你和工具在同一台电脑上、WorkBuddy 可访问该目录），它可以比上面更省事：

1. 你照常用大白话把竞品数据发给 WorkBuddy。
2. 它会按同一套 JSON 规则整理，并**直接写入本分享版目录下的 `data.js`**（与本 html 同目录的那个）；建议同时留一份本地 JSON 备份（如 `竞品_<账号>_<日期>.json`）。
3. **写入后必须让工具重新加载一次才生效**：在工具里点「清空本地数据」→ 再点「同步库数据」（或清空后刷新页面）。因为工具打开时**优先读浏览器 localStorage**，只改 data.js 不会自动显示。
4. ⚠️ **只写本分享版目录下的 data.js**。不要去碰任何"主版/主目录"的 data.js——本分享版不依赖它们，数据也不互通、不同步。WorkBuddy 定位文件时认准：与 `竞品分析工具_分享版_空模板.html` 在同一个文件夹里的那个 `data.js`。
5. 如果你是从 GitHub 下载的分享版，data.js 路径就是「你下载解压后那个文件夹里的 data.js」，WorkBuddy 告诉它这个文件夹的完整路径即可。

> 和标准「方式一」的区别：标准方式 = WorkBuddy 只返回 JSON、由你手动「导入JSON」（适合 WorkBuddy 碰不到工具文件夹的情况）；增强方式 = WorkBuddy 直接写 data.js（适合有访问权限的情况）。两种方式最终都需要工具页面执行一次加载。

### 方式二：自己在界面手动录入

适合少量数据、或想边看边填的情况。

#### 1. 竞品账号库（先建账号）

点顶部「竞品账号库」标签 → 点「新增账号」，填写：

- **账号名称**：视频号昵称，如「六月人物漫谈」
- **平台**：填「视频号」
- **首次发现**：你第一次注意到这个账号的日期
- **发现时已开通（天）**：首次发现那天，它的橱窗已经开通了多少天（不知道就填 0）
- **主页链接 / 橱窗链接**：微信小店橱窗地址，格式如 `#微信小店带货橱窗://账号名的橱窗/xxxx`
- **粉丝量 / 内容领域 / 主要商品 / 账号定位**：按实际情况填
- 填完点「保存」。

> 表格里的「开通时间」列会自动显示「发现时已开通 + 距今天数」，每天自己长大，不用手填。
> 「橱窗链接」列右侧有 **复制** 按钮，点一下即可复制该账号的微信小店链接。

#### 2. 每日数据记录（每天填销量）

点「每日数据记录」标签 → 点「新增记录」，填写：

- **日期**：哪一天的销量
- **账号**：从下拉里选刚建好的账号
- **视频标题 / 视频类型**：可选，方便回忆
- **商品**：当天推的书。名字暂时确认不了就填「待确认」
- **当前销量**：当天橱窗显示的累计已售件数（从截图看）
- **昨日销量**：前一天的累计已售件数

保存后，**「新增销量」不用你算**，系统自动 = 当前销量 − 昨日销量。其中「发现当天」（日期等于账号的首次发现日）新增销量自动记 0。

> 例：8/15 当前 439、昨日 0 → 新增 0；8/16 当前 458、昨日 439 → 新增 19。

#### 3. 爆款案例库（可选）

遇到特别好的视频，点「爆款案例库」标签 → 「新增案例」，记录它的开头 3 秒、内容结构、情绪点、成交效果，以及你觉得**可复制的经验**，方便以后借鉴。

#### 方式三：每日采集 → 批量录入（推荐日常流程）

每天要在微信里挨个点开竞品橱窗看销量、再回填，这套流程已经内置在「每日数据记录」标签里，不用每次找人帮忙：

1. **导出采集清单**：在「每日数据记录」标签，点工具栏的 **导出采集清单**，弹框里点 **复制全部**，即可一次性复制所有账号的微信小店橱窗链接（也可点 **下载txt** 存成文件）。把链接发到微信「文件传输助手」，在微信里逐个点开，记下每个账号当天的当前销量。
2. **批量新增记录**：回到工具，点 **批量新增记录**，在弹框里粘贴你记下的销量，每行一个账号：

   ```
   账号名（内容领域 / 主要商品） 当前销量
   ```

   例如：`示例账号甲（历史人物 / 国之脊梁） 444`。可附带橱窗链接（同一行末尾空格后粘贴 `#微信小店带货橱窗://...`），系统会顺带校验链接是否对得上。
3. 点 **解析预览**：系统会自动算出每个账号的「昨日销量 / 新增销量」，并标出异常（负值、账号库未找到、链接不符、领域/商品待确认、异常大涨），请先核对。
4. 勾选要写入的行（默认全勾），点 **确认写入**，数据即进入「每日数据记录」。同一账号同一天若已存在记录会自动更新，不会重复。

> 提示：「当前销量」填当天橱窗显示的**累计已售件数**；「新增销量」系统按 `当前 − 昨日` 自动算，你不用手填。

---

## 四、看数据分析

点「数据看板」标签，三块内容：

1. **增长最快账号**：默认看「近 1 天」累计新增销量排名。框里可以改成 7 天、30 天等。排行榜每一行右侧都有 **复制地址** 按钮，点一下即可复制该账号的微信小店橱窗链接，方便直接去微信查看。
2. **按日期增长趋势**：折线图，横轴日期、纵轴销量。上方可切换「账号」（看单个或看全部合计）和「指标」（新增销量 / 当前销量 / 播放量）。
3. **销量最高商品**：按商品最新累计销量排 TOP 10。

---

## 五、数据保存与备份

- **自动保存**：你每次新增 / 编辑 / 删除，数据都自动存在**本机浏览器**里。关掉网页再打开，数据还在。
- **备份（重要）**：换电脑、重装浏览器、清缓存前，点右上角「导出JSON」，会下载一个 json 文件，存好它。
- **恢复**：到新环境打开工具，点「导入JSON」，选那个 json 文件即可。
- **清空**：点「清空本地数据」会把本地所有数据清掉（含示例），清空后刷新页面**不会自动恢复**，是真清空；想找回示例就点「同步库数据」。

---

## 六、常见问题

**Q：打开是空白页？**
A：检查 `html` 和 `data.js` 是否在同一文件夹；用 Chrome / Edge 打开；若之前打开过旧版，清一下浏览器缓存再试。

**Q：新增销量算出来不对？**
A：它 = 当前销量 − 昨日销量。请确认「昨日销量」填的是前一天的累计数，不是当天的新增数。发现当天会自动记 0。

**Q：数据会不会被别人看到 / 上传？**
A：不会。纯本地工具，没有任何网络请求，数据只存在你自己浏览器里。

**Q：多人想共用一份数据？**
A：本工具没有云端。做法：某人「导出JSON」发给大家，其他人「导入JSON」加载同一份。

---

## 七、注意事项

- 这是**本地单机工具**，没有账号系统、没有云端同步，**数据不与任何主版本/原作者互通**。你录入的数据只存在你自己的浏览器里。
- 数据只认浏览器：同一台电脑换浏览器、或清了站点数据，记录会丢——记得常「导出JSON」备份。
- 示例数据（只有一个「六月人物漫谈」账号）只是示范格式，看清后点「清空本地数据」即可开始你自己的记录。
- 如果由 WorkBuddy 帮你写 data.js，它只应写**本分享版目录下**的 data.js（与 html 同目录），不要碰任何主版/主目录的 data.js——两者数据独立、不同步。
